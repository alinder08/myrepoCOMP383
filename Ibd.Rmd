library(ggplot2)
#read in .genome file as a table
ibd = read.table("plink.genome", header = T)
#create a plot of Z0 vs Z1
qplot(Z0,Z1,data=ibd, colour=RT) + scale_colour_manual(value=c(3,"darkorange",4,2,1))
#histogram of the pi_hat values
hist(ibd$PI_HAT)
